#!/usr/bin/env python

# Traditional installs are handled by pyproject.toml.
# This setup.py enables editable installs via `pip install -e .`

import setuptools

if __name__ == "__main__":
    setuptools.setup()
